layer: "library"
language: "typescript"

project:
  name: "dataset-delta-t"
  description: "Delta-T dataset snapshot and provider factory."
  owner: "lzm0x219"
  maintainers: ["lzm0x219"]

tasks:
  build:
    command: "tsdown"
    inputs:
      - "@globs(sources)"
      - "tsdown.config.ts"
    outputs:
      - "dist"
