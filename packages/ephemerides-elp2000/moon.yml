layer: "library"
language: "typescript"

project:
  name: "ephemerides-elp2000"
  description: "Minimal lunar ephemeris provider."
  owner: "lzm0x219"
  maintainers: ["lzm0x219"]

tasks:
  build:
    command: "tsdown"
    inputs:
      - "@globs(sources)"
      - "tsdown.config.ts"
    outputs:
      - "dist"
