layer: "library"
language: "typescript"

project:
  name: "temporal"
  description: "Time scales, Julian Day, instants, and provider boundaries."
  owner: "lzm0x219"
  maintainers: ["lzm0x219"]

tasks:
  build:
    command: "tsdown"
    inputs:
      - "@globs(sources)"
      - "tsdown.config.ts"
    outputs:
      - "dist"
