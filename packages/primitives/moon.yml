layer: "library"
language: "typescript"

project:
  name: "primitives"
  description: "Foundational immutable values and numeric helpers."
  owner: "lzm0x219"
  maintainers: ["lzm0x219"]

tasks:
  build:
    command: "tsdown"
    inputs:
      - "@globs(sources)"
      - "tsdown.config.ts"
    outputs:
      - "dist"
