layer: "library"
language: "typescript"

project:
  name: "reference"
  description: "Coordinate and reference-frame value objects."
  owner: "lzm0x219"
  maintainers: ["lzm0x219"]

tasks:
  build:
    command: "tsdown"
    inputs:
      - "@globs(sources)"
      - "tsdown.config.ts"
    outputs:
      - "dist"
