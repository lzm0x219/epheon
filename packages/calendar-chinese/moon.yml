layer: "library"
language: "typescript"

project:
  name: "calendar-chinese"
  description: "Chinese calendar rules."
  owner: "lzm0x219"
  maintainers: ["lzm0x219"]

tasks:
  build:
    command: "tsdown"
    inputs:
      - "@globs(sources)"
      - "tsdown.config.ts"
    outputs:
      - "dist"
