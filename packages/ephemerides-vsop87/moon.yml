layer: "library"
language: "typescript"

project:
  name: "ephemerides-vsop87"
  description: "Minimal solar ephemeris provider."
  owner: "lzm0x219"
  maintainers: ["lzm0x219"]

tasks:
  build:
    command: "tsdown"
    inputs:
      - "@globs(sources)"
      - "tsdown.config.ts"
    outputs:
      - "dist"
