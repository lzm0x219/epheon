# 第六阶段任务清单：兼容与工具

对应计划：`docs/plans/0006-stage-6-compatibility-and-tooling.md`

## 任务总览

| ID    | 状态 | 任务                                 | 主要输出                   |
| ----- | ---- | ------------------------------------ | -------------------------- |
| S6-T1 | 暂缓 | 设计 `@epheon/cli` 最小只读命令集    | `packages/cli/` 或等价入口 |
| S6-T2 | 暂缓 | 设计 `@epheon/compat` 外部格式适配层 | `packages/compat/`         |
| S6-T3 | 暂缓 | 设计 `@epheon/runtime` 运行时装配层  | `packages/runtime/`        |

## S6-T1：设计 `@epheon/cli` 最小只读命令集

状态：`暂缓`

进入条件：

- 第四阶段已经给出稳定的中国历法公共 API。
- 第五阶段已经给出可复核样例与基准。
- 出现真实的命令行查询需求。

完成条件：

- 先只支持少量只读查询，不提前扩成完整应用层。

## S6-T2：设计 `@epheon/compat` 外部格式适配层

状态：`暂缓`

进入条件：

- 出现明确的外部接入格式或互操作需求。

完成条件：

- 只适配真实需要的格式，不为未来猜测建抽象。

## S6-T3：设计 `@epheon/runtime` 运行时装配层

状态：`暂缓`

进入条件：

- provider、dataset、calendar 查询已经需要统一装配入口。

完成条件：

- 只承载运行时装配，不反向侵入核心领域层。
